spring:
  application:
    name: Produtos API
  datasource:
    url: jdbc:mysql://localhost:3306/produtos_db?createDatabaseIfNotExist=true&serverTimezone=UTC
    username: root
    password: Semsenha1877@
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true